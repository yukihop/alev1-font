{
  "name": "font",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build.ts",
    "build:glyphs": "node src/generate-glyphs.ts",
    "build:features": "node src/generate-features.ts",
    "build:metadata": "node src/build-metadata.ts",
    "build:preview": "node src/build-preview.ts",
    "build:font": "node src/build-font.ts",
    "check": "node src/check.ts",
    "clean": "node src/clean.ts"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
