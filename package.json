{
  "name": "alevish-font",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "engines": {
    "node": "24.6"
  },
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "release:package": "node scripts/package-font-release.ts",
    "build": "pnpm --filter font build && pnpm --filter docs build",
    "check": "pnpm -r --filter font --filter docs check"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
